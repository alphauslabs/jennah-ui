version: v1
plugins:
  # Generates the types (Requests, Responses, Messages)
  - plugin: buf.build/bufbuild/es
    out: src/gen
    opt: target=ts
  # Generates the client services (submitJob, listJobs)
  - plugin: buf.build/connectrpc/es
    out: src/gen
    opt: target=ts